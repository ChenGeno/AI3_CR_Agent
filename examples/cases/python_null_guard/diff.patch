diff --git a/service/user_service.py b/service/user_service.py
index 1111111..2222222 100644
--- a/service/user_service.py
+++ b/service/user_service.py
@@ -20,3 +20,3 @@
 
 def fetch_profile_name(command: str) -> str:
-    return subprocess.check_output(command.split(), text=True).strip()
+    return subprocess.check_output(command, shell=True, text=True).strip()
@@ -25,12 +25,12 @@
 def update_user_status(user: User, raw_status: Optional[str], command: str) -> dict[str, str]:
     status = normalize_status(raw_status)
     try:
-        profile_name = user.profile.name.strip() if user.profile and user.profile.name else ""
+        profile_name = user.profile.name.strip()
         external_name = fetch_profile_name(command)
         return {
             "user_id": str(user.id),
             "status": status,
             "profile_name": profile_name or external_name,
         }
-    except (RuntimeError, ValueError) as exc:
+    except Exception as exc:
         return {"user_id": str(user.id), "status": status, "error": str(exc)}
